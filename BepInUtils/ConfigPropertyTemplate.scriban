public delegate void On{{Key}}ValueChangedEvent({{Type}} newValue);

public static event On{{Key}}ValueChangedEvent? On{{Key}}ValueChanged;

public static {{Type}} {{Key}}
{
    get => _{{Key}}?.Value ?? default;
    set
    {
        if (_{{Key}} == null) return;
        _{{Key}}.Value = value;
    }
}